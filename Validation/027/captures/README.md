# Feature 027 Captures

Accepted captures prove visible native Metal presentation and retain orientation,
extent, colorspace, lifecycle-cycle count, source revision, and SHA-256 evidence.
Local exploratory captures belong in the ignored `local/` directory. Only a
small accepted capture and normalized metadata are checked in.
