# Feature 027 CI Evidence

This file will record hosted build/cook and required GPU-capable hardware run
IDs, head revisions, runner labels, probe conclusions, artifact names, and
SHA-256 digests. Downloaded artifacts remain in the ignored `downloaded/`
directory. An unavailable hosted Metal-device probe is diagnostic evidence and
does not satisfy either required native hardware lane.
