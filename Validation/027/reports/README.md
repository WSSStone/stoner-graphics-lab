# Feature 027 Reports

Reports are canonical JSON or compact Markdown summaries named by milestone and
workload. They must identify their evidence tier and may report `unavailable`
without treating it as a pass. Absolute developer paths, native pointer values,
timestamps in identity-bearing fields, and unbounded command output are
forbidden.
