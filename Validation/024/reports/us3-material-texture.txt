Feature 024 US3 Material And Texture Acceptance
Date: 2026-08-14

Gates:
  conda run -n godot scons platform=macos config=debug strict=true -j8
  Build/Mac/Debug/Tests/StonerTest --suite asset-gltf-material
  Build/Mac/Debug/Tests/StonerTest --suite asset
  conda run -n godot scons platform=macos config=release strict=true -j8
  Build/Mac/Release/Tests/StonerTest --suite asset-gltf-material
  Build/Mac/Release/Tests/StonerTest --suite asset
  conda run -n godot python Tests/verify_static_model_provenance.py
  git diff --check

Result: PASS

Accepted coverage:
- Metallic-roughness base color, metallic, roughness, emissive, normal scale,
  occlusion strength, alpha mask/cutoff, and double-sided state.
- Schema-v2 typed Texture bindings with TEXCOORD_1 and nearest/linear,
  mirrored-repeat, and clamp-to-edge sampler intent.
- Package-local glTF default material with canonical core defaults.
- Embedded PNG and resolver-only source-relative JPEG dependencies.
- Shared Image output with distinct sRGB color and linear normal/data Texture
  identities and exact complete package identity validation.
- Twenty repeated imports with deterministic package inspection evidence.

Atomic failure coverage:
- Missing external JPEG returns NotFound with no package outputs.
- A material binding that requires absent TEXCOORD_1 returns MalformedSource
  with no package outputs.

Repository migration:
- DeferredSurface material now uses schema v2 structured texture bindings.
- DeferredSurface shader required-parameter vocabulary matches the active glTF
  core mapping profile without changing the shader logical identity.
