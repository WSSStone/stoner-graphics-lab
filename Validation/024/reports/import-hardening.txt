Feature 024 Bounded Import Hardening
Date: 2026-08-14

Gates:
  conda run -n godot scons platform=macos config=debug strict=true -j8
  Build/Mac/Debug/Tests/StonerTest --suite asset-gltf-hardening
  Build/Mac/Debug/Tests/StonerTest --suite asset
  conda run -n godot scons platform=macos config=release strict=true -j8
  Build/Mac/Release/Tests/StonerTest --suite asset-gltf-hardening
  Build/Mac/Release/Tests/StonerTest --suite asset
  conda run -n godot python -m unittest
    Tests/test_generate_static_model_invalid_fixtures.py
    Tests/test_verify_static_model_provenance.py
  conda run -n godot python Tests/verify_static_model_provenance.py
  git diff --check

Result: PASS

Hardening evidence:
- Malformed JSON, non-triangle topology, required extensions, morph targets,
  skins, unsupported semantics, parser allocation exhaustion, count/depth,
  decoded geometry, dependency, and diagnostic limits fail closed.
- Dependency resolution rejects traversal, percent ambiguity, schemes,
  fragments, self-resolution, and resolver aliases before publication.
- Data URI decoding validates base64 structure, padding placement, and
  canonical unused bits under a finite output budget.
- Main, per-dependency, aggregate dependency, parser allocation, hierarchy,
  canonical geometry, and diagnostic budgets are finite and checked.
- Failed imports preserve pre-existing caller outputs and leave registry state
  unchanged; complete package outputs replace caller state only after package
  identity, payload, and dependency validation succeeds.
- Normalized diagnostics redact absolute paths and use stable stage, subject,
  field, result, and reason records.
- Optional extension and excluded camera/light/animation counts are retained in
  deterministic package inspection attributes.
- The deterministic invalid-fixture generator produced identical classified
  manifests in independent temporary directories.
