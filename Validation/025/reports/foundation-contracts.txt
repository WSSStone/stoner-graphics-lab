Feature 025 Foundation Contracts Gate
====================================

Date: 2026-08-14
Platform: macOS arm64
Environment: conda godot, SCons 4.10.1, C++20
Scope: Tasks T007-T044 / Plan milestones M0-M2

Debug gates
-----------

PASS  core-file-transaction
  Bounded regular-file queries and enumeration, canonical containment,
  symlink escape rejection, same-volume move, durable write, atomic replace,
  UTF-8/long paths, permissions, and contained removal.

PASS  core-file-lease
  Native ownership, readable owner metadata, bounded same/cross-process
  contention, move ownership, crash release, and non-inheritance.

PASS  asset-cooker-profile
  Strict canonical profile parsing, normalized effective identity, relevant
  producer projection, schema/ordering validation, and KTX2 migration.

PASS  asset-cooker-derived-key
  Complete evidence, domain/type/length separation, relevant invalidation,
  display-name exclusion, and checked-in golden key.

PASS  asset-cooker-codec
  SGCOOK01 little-endian envelope, digest/limit/corruption handling, canonical
  manifest and generation identity, checked-in valid/malformed goldens, all ten
  Feature 021-024 typed families, source/cooked semantic equivalence,
  byte-identical rewrite, valid-digest truncated-body rejection, atomic
  extension registration, and producer-settings fail-closed behavior.

PASS  asset
  Complete Asset regression suite, including Features 020-024 and the new
  Feature 025 profile, key, envelope, manifest, and typed-codec tests.

Release gate
------------

PASS  strict Release build with -Wall -Wextra -Werror
PASS  Release asset-cooker-codec
PASS  Release core-file-transaction
PASS  Release core-file-lease

Contract tooling
----------------

PASS  Tests/verify_asset_cooker_contracts.py
PASS  Tests/test_verify_asset_cooker_contracts.py (4 tests)

The verifier checks schema closure, fixture provenance and SHA-256 records,
SGCOOK01 boundaries/digests, canonical manifest bytes, effective-profile
identity, and semantic generation identity. C++ tests independently consume
the generated goldens, preventing a self-validating generator from defining
success alone.

Result: PASS
