# Asset Cooker Report Fixtures

`valid-plan.json` is a normalized report generated by the Feature 025 CLI from
the repository representative corpus. `invalid-profile.json` is the canonical
diagnostic report produced when profile decoding fails. Both are deterministic
contract fixtures; host timing and memory telemetry are intentionally absent.
