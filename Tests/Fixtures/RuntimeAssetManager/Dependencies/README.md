# Dependency Fixture Contract

Dependency fixtures cover required chains, shared children, cycles, missing
nodes, declared validated optional fallbacks, and undeclared or unsatisfied
soft edges. Identities and edges are kept in canonical sorted order.

