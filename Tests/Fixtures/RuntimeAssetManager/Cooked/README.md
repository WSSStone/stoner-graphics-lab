# Cooked Fixture Contract

Cooked runtime fixtures are published into ignored per-test work roots using
the Feature 025 publisher. Corruption variants change one manifest-listed
field or payload at a time. Strict cooked tests must never configure a source
root or invoke a resolver/importer.

