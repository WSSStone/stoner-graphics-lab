#version 450

layout(local_size_x = 4, local_size_y = 1, local_size_z = 1) in;
layout(set = 0, binding = 0, std430) buffer Values
{
    uint Data[];
} ValuesBuffer;

void main()
{
    uint Index = gl_GlobalInvocationID.x;
    ValuesBuffer.Data[Index] = ValuesBuffer.Data[Index] * 2u + Index;
}
