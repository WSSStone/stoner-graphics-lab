# Metal Conformance Fixtures

Authoritative SPIR-V and expected graphics, compute, transfer, synchronization,
and readback values live here. Inputs are repository-owned shader products from
the existing GLSL/SPIR-V chain. Each case names the RHI operation rows it covers
and must never contain semantic-oracle bytes presented as GPU output.
